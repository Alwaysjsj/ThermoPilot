<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/UI;component/Theme/MainColor.xaml" />
    </ResourceDictionary.MergedDictionaries>


    <!-- 自定义滑块样式 -->
    <Style x:Key="ColoredThumbStyle"
           TargetType="Thumb">

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Border Background="{StaticResource SecondBackColor}"
                            CornerRadius="4" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>

    </Style>


    <!-- ScrollBar 通用样式 -->
    <Style x:Key="CommonScrollBarStyle"
           BasedOn="{StaticResource {x:Type ScrollBar}}"
           TargetType="ScrollBar">

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ScrollBar}">

                    <Track x:Name="PART_Track"
                           IsDirectionReversed="True">

                        <Track.DecreaseRepeatButton>
                            <RepeatButton Background="{StaticResource SecondForeColor}"
                                          Command="ScrollBar.PageUpCommand" />
                        </Track.DecreaseRepeatButton>

                        <Track.Thumb>
                            <Thumb Style="{StaticResource ColoredThumbStyle}" />
                        </Track.Thumb>

                        <Track.IncreaseRepeatButton>
                            <RepeatButton Background="{StaticResource SecondForeColor}"
                                          Command="ScrollBar.PageDownCommand" />
                        </Track.IncreaseRepeatButton>

                    </Track>

                </ControlTemplate>
            </Setter.Value>
        </Setter>

    </Style>

</ResourceDictionary>