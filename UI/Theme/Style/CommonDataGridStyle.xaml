<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/UI;component/Theme/MainColor.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <Style x:Key="CommonDataGridStyle"
           TargetType="DataGrid"
           BasedOn="{StaticResource {x:Type DataGrid}}">

        <Setter Property="Background"
                Value="{StaticResource AreaBackColor}"/>
        <Setter Property="BorderThickness"
                Value="1"/>
        <Setter Property="CanUserAddRows"
                Value="False"/>
        <Setter Property="AutoGenerateColumns"
                Value="False"/>
        <Setter Property="RowHeaderWidth"
                Value="0"/>
        <Setter Property="IsReadOnly"
                Value="True"/>
        <Setter Property="SelectionMode"
                Value="Single"/>
        <Setter Property="ColumnHeaderStyle">
            <Setter.Value>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background"
                            Value="{StaticResource SecondBackColor}"/>
                    <Setter Property="Foreground"
                            Value="{StaticResource SecondForeColor}"/>
                    <Setter Property="BorderBrush"
                            Value="{StaticResource SecondForeColor}"/>
                    <Setter Property="BorderThickness"
                            Value="1"/>
                    <Setter Property="HorizontalContentAlignment"
                            Value="Center"/>
                    <Setter Property="FontSize"
                            Value="{StaticResource SecondFontSize}"/>
                    <Setter Property="Height"
                            Value="30"/>
                    <Setter Property="Padding"
                            Value="2,0"/>
                </Style>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="GridViewColumnHeaderStyle"
           BasedOn="{StaticResource {x:Type GridViewColumnHeader}}"
           TargetType="GridViewColumnHeader">

        <Setter Property="Background"
                Value="{StaticResource SecondBackColor}"/>
        <Setter Property="TextElement.FontSize"
                Value="{StaticResource ContentFontSize}"/>
        <Setter Property="TextElement.Foreground"
                Value="{StaticResource SecondForeColor}"/>
    </Style>

    <Style TargetType="GridSplitter">

        <Setter Property="Background"
                Value="{StaticResource SecondBackColor}"/>
        <Setter Property="HorizontalAlignment"
                Value="Center"/>
        <Setter Property="VerticalAlignment"
                Value="Stretch"/>
        <Setter Property="Width"
                Value="5"/>
        <Setter Property="Height"
                Value="Auto"/>
        <Setter Property="BorderThickness"
                Value="0"/>
        <Setter Property="ShowsPreview"
                Value="True"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver"
                     Value="True">

                <Setter Property="Background"
                        Value="{StaticResource MouseOverLightColor}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="ModernDataGridRowStyle"
           TargetType="DataGridRow">

        <Style.Triggers>
            <Trigger Property="IsMouseOver"
                     Value="True">

                <Setter Property="Background"
                        Value="{StaticResource MouseOverLightColor}"/>
            </Trigger>
            <Trigger Property="IsSelected"
                     Value="True">

                <Setter Property="Background"
                        Value="{StaticResource SelectedColor}"/>
                <Setter Property="Foreground"
                        Value="{StaticResource FirstForeColor}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="ModernDataGridCellStyle"
           TargetType="DataGridCell">

        <Style.Resources>
            <Style x:Key="DataGridLabelStyle"
                   TargetType="Label">

                <Setter Property="Foreground"
                        Value="{Binding RelativeSource={RelativeSource AncestorType=DataGridCell},Path=Foreground}"/>
            </Style>

            <Style TargetType="Label"
                   BasedOn="{StaticResource DataGridLabelStyle}">

                <Setter Property="VerticalAlignment"
                        Value="Center"/>
                <Setter Property="VerticalContentAlignment"
                        Value="Center"/>
                <Setter Property="HorizontalAlignment"
                        Value="Center"/>
                <Setter Property="HorizontalContentAlignment"
                        Value="Center"/>
            </Style>
        </Style.Resources>

        <Setter Property="BorderThickness"
                Value="0"/>

        <Style.Triggers>
            <Trigger Property="IsSelected"
                     Value="True">

                <Setter Property="Background"
                        Value="{StaticResource SelectedColor}"/>
                <Setter Property="Foreground"
                        Value="{StaticResource FirstForeColor}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="ModernSortableDataGridHeaderStyle"
           TargetType="DataGridColumnHeader">

        <Setter Property="HorizontalContentAlignment"
                Value="Center"/>
        <Setter Property="VerticalContentAlignment"
                Value="Center"/>
        <Setter Property="Height"
                Value="Auto"/>
        <Setter Property="MinHeight"
                Value="30"/>
        <Setter Property="Padding"
                Value="5,0"/>
        <Setter Property="Foreground"
                Value="{StaticResource SecondForeColor}"/>
        <Setter Property="Background"
                Value="{StaticResource SecondBackColor}"/>
        <Setter Property="BorderThickness"
                Value="1"/>
        <Setter Property="BorderBrush"
                Value="{StaticResource SecondForeColor}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGridColumnHeader">
                    <Border BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">

                        <Grid Background="{TemplateBinding Background}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <ContentPresenter Grid.Column="0"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              RecognizesAccessKey="True"
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>

                            <Grid Grid.Column="1"
                                  Margin="5,0,5,0">

                                <Path x:Name="SortAscArrow"
                                      Width="8"
                                      Height="8"
                                      VerticalAlignment="Center"
                                      Fill="{StaticResource SecondForeColor}"
                                      Stretch="Fill"
                                      Visibility="Collapsed"
                                      Data="M 0 5 L 5 0 L 10 5 Z"/>

                                <Path x:Name="SortDescArrow"
                                      Width="8"
                                      Height="8"
                                      VerticalAlignment="Center"
                                      Fill="{StaticResource SecondForeColor}"
                                      Stretch="Fill"
                                      Visibility="Collapsed"
                                      Data="M 0 0 L 5 5 L 10 0 Z"/>
                            </Grid>
                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="SortDirection"
                                 Value="Ascending">

                            <Setter TargetName="SortAscArrow"
                                    Property="Visibility"
                                    Value="Visible"/>

                            <Setter TargetName="SortDescArrow"
                                    Property="Visibility"
                                    Value="Collapsed"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ModernDataGridStyle"
           BasedOn="{StaticResource CommonDataGridStyle}"
           TargetType="DataGrid">

        <Setter Property="RowBackground"
                Value="{StaticResource AreaBackColor}"/>
        <Setter Property="RowStyle"
                Value="{StaticResource ModernDataGridRowStyle}"/>
        <Setter Property="CellStyle"
                Value="{StaticResource ModernDataGridCellStyle}"/>
    </Style>
</ResourceDictionary>