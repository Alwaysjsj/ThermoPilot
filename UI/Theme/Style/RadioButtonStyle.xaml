<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/UI;component/Theme/MainColor.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style x:Key="CommonRadioButtonStyle"
           BasedOn="{StaticResource {x:Type RadioButton}}"
           TargetType="RadioButton">

        <Setter Property="Background"
                Value="{StaticResource SecondBackColor}" />
        <Setter Property="Foreground"
                Value="{StaticResource FirstForeColor}" />

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <Grid Background="{TemplateBinding Background}">

                        <Ellipse x:Name="RadioButtonEllipse"
                                 Width="16"
                                 Height="16"
                                 Margin="2,0"
                                 Fill="{StaticResource SecondBackColor}"
                                 Stroke="{StaticResource SecondBackColor}"
                                 VerticalAlignment="Center"
                                 HorizontalAlignment="Center"
                                 StrokeThickness="1" />

                        <ContentPresenter Margin="20,0,0,0"
                                          HorizontalAlignment="Left"
                                          VerticalAlignment="Center"
                                          RecognizesAccessKey="True" />
                    </Grid>

                    <ControlTemplate.Triggers>

                        <Trigger Property="IsChecked"
                                 Value="True">
                            <Setter TargetName="RadioButtonEllipse"
                                    Property="Fill"
                                    Value="{StaticResource SelectedColor}" />
                        </Trigger>

                        <Trigger Property="IsChecked"
                                 Value="False">
                            <Setter TargetName="RadioButtonEllipse"
                                    Property="Fill"
                                    Value="{StaticResource SecondBackColor}" />
                        </Trigger>

                        <Trigger Property="IsEnabled"
                                 Value="False">
                            <Setter TargetName="RadioButtonEllipse"
                                    Property="Fill"
                                    Value="{StaticResource DisabledColor}" />
                            <Setter Property="Foreground"
                                    Value="{StaticResource DisabledColor}" />
                        </Trigger>

                        <Trigger Property="IsMouseOver"
                                 Value="True">
                            <Setter TargetName="RadioButtonEllipse"
                                    Property="Fill"
                                    Value="{StaticResource MouseOverColor}" />
                        </Trigger>

                        <Trigger Property="IsPressed"
                                 Value="True">
                            <Setter TargetName="RadioButtonEllipse"
                                    Property="Fill"
                                    Value="{StaticResource SelectedColor}" />
                        </Trigger>

                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <Style x:Key="ModernRadioButtonStyle"
           TargetType="RadioButton">

        <Setter Property="Background"
                Value="Transparent" />
        <Setter Property="Foreground"
                Value="{StaticResource FirstBackColor}" />

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <Grid>
                        <StackPanel Orientation="Horizontal">

                            <TextBlock x:Name="icon"
                                       Margin="5"
                                       FontSize="20"
                                       Text="○"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Center" />

                            <ContentPresenter VerticalAlignment="Center" />

                        </StackPanel>
                    </Grid>

                    <ControlTemplate.Triggers>

                        <Trigger Property="IsChecked"
                                 Value="True">
                            <Setter TargetName="icon"
                                    Property="Text"
                                    Value="●" />
                            <Setter TargetName="icon"
                                    Property="Foreground"
                                    Value="{StaticResource SelectedColor}" />
                        </Trigger>

                        <Trigger Property="IsMouseOver"
                                 Value="True">
                            <Setter TargetName="icon"
                                    Property="Foreground"
                                    Value="{StaticResource MouseOverColor}" />
                        </Trigger>

                        <Trigger Property="IsEnabled"
                                 Value="False">
                            <Setter TargetName="icon"
                                    Property="Opacity"
                                    Value="0.5" />
                        </Trigger>

                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <Style TargetType="RadioButton"
           BasedOn="{StaticResource CommonRadioButtonStyle}" />

</ResourceDictionary>